# 课程总览：s01–s20 面试深度版

> 用法：先看每章"一句话"，再看"新增功能/实现方法"细节，最后用"面试追问"自测。

## 主线脉络

- 第一轮 · 主干架构（s01–s08）：循环 → 工具 → 权限 → hooks → 计划 → 子 Agent → 技能 → 压缩
- 第二轮 · 系统可用性（s09–s12）：记忆 → prompt 组装 → 错误恢复 → 任务系统
- 第三轮 · 长期运行与扩展（s13–s20）：后台 → 定时 → 团队 → 协议 → 自治 → 隔离 → 插件 → 综合

---

## s01 Agent Loop —— 一个循环就够了

**一句话**：让模型"输出命令 → 执行 → 看结果 → 继续推理"循环起来。

**新增功能**
- `agent_loop(messages)` 主循环
- 第一个工具 `bash`（含 schema）
- tool_result 回填机制

**解决的问题**：模型只会输出一条 bash 命令，输出完就停了——它不会自己执行，也不会看到结果后继续推理。

**实现方法**
1. `TOOLS` 定义 bash 的 JSON schema，随请求发给 API。
2. 循环：`client.messages.create(model, system, messages, tools)`。
3. 把 assistant 回复 append 进 `messages`。
4. 若 `response.stop_reason == "tool_use"`：遍历 content blocks，执行 `run_bash(block.input["command"])`。
5. 把执行结果以 `{"type": "tool_result", "tool_use_id": block.id, "content": out}` 作为 **user 消息**回填。
6. 回到第 2 步；`stop_reason != "tool_use"`（end_turn / max_tokens）时退出。

**关键点**：所有状态都在 `messages` 里（LLM 无持久状态）；`tool_use_id` 把请求和结果配对；`stop_reason` 是循环终止信号。

**面试追问**
- 为什么 tool_result 必须回填成 user 消息？——Messages API 的协议约定：工具结果作为新输入回合交给模型。
- 模型一次输出多个 tool_use 怎么办？——遍历 content blocks，逐个执行、逐个回填，合并成一条 user 消息。
- stop_reason 有哪几种？——tool_use / end_turn / max_tokens（s11 专门处理第三种）。

---

## s02 Tool Use —— 多加一个工具，只加一行

**一句话**：工具注册表 + 分发 map，循环体稳定不变。

**新增功能**
- 5 个工具：bash / read_file / write_file / edit_file / glob
- `TOOL_HANDLERS` 分发字典：`block.name → handler`
- `safe_path()` 路径安全校验

**解决的问题**：每加一个工具都要改循环，循环膨胀。

**实现方法**
1. 每个工具两处注册：`TOOLS`（schema，给模型看）+ `TOOL_HANDLERS`（handler，给代码用）。
2. 循环统一：`handler = TOOL_HANDLERS.get(block.name); output = handler(**block.input)`。
3. `safe_path`：`(WORKDIR / p).resolve()` 后检查 `is_relative_to(WORKDIR)`，防止路径逃逸。

**关键点**：schema 是模型与 harness 的契约——描述写得好，模型才调得对；dispatch map 让"加工具"变成纯增量。

**面试追问**
- 新工具要改几个地方？——两处（schema + handler），循环零改动。
- safe_path 够安全吗？——不够，教学示意（字符串匹配可绕过）；s03 把它上移到权限层，生产靠多阶段管线。

---

## s03 Permission —— 执行前做权限判断

**一句话**：三道闸门数据驱动，安全职责从工具内上移到权限层。

**新增功能**
- 权限规则表 `PERMISSION_RULES`（数据驱动，如 bash 里的敏感词规则）
- `DENY_LIST` 直接禁止列表
- `check_permission(block)` 三道闸门管线

**解决的问题**：bash 不受限，"清理项目"可能执行 `rm -rf /`。安全不能靠信任模型，要靠代码。

**实现方法（三道闸门，顺序不可换）**
1. **直接禁止**：命中 deny 规则 → 拦截，返回原因。
2. **命中询问规则** → 问用户是否允许。
3. **通过**：默认放行，保证正常日常行为不被频繁打断。

拦截时构造 tool_result 回填给模型（"为什么被拒 + 建议"），模型据此调整下一步。

**关键点**：加一条新权限规则 = 改规则表一处（数据驱动）；安全从工具内部（s02 safe_path）上移到权限层，所有工具统一过闸。

**面试追问**
- 三道闸门顺序为什么不能换？——deny 是策略底线，必须最先；ask 只兜住"规则明确要问"的；放行兜底。换了会让危险命令有机会先被放行。
- 为什么字符串 deny list 不是可靠安全机制？——命令变体和 shell 展开可绕过（`rm -rf / ; echo hi`）。教学示意，生产靠 CC 的多阶段解析管线。
- 数据驱动的好处？——规则集中管理、可扩展、可配置化，不用改循环。

---

## s04 Hooks —— 挂在循环上，不写进循环里

**一句话**：事件注册表 + 短路触发，扩展 = 注册回调。

**新增功能**
- `HOOKS` 注册表：事件名 → 回调列表
- `register_hook(event, callback)` / `trigger_hooks(event, *args)`
- 四个事件：UserPromptSubmit（输入后进 LLM 前）、PreToolUse（工具前）、PostToolUse（工具后）、Stop（退出前）
- 教学示例 hook：log_hook、large_output_hook、context_inject_hook、summary_hook
- 后续改造：permission_hook 薄壳 + `stopHookActive` 防无限循环

**解决的问题**：每加一个检查/日志/通知都要改 agent_loop，循环膨胀成"一锅粥"。

**实现方法**
1. 循环在关键节点调 `trigger_hooks`，由注册表决定跑什么。
2. 返回值约定：None = 放行/继续；非 None = 有干预（PreToolUse 拦截并回填原因；Stop 强制续跑）。
3. 短路：第一个非 None 直接返回，后面 hook 不执行——**注册顺序 = 执行顺序，且敏感**。
4. `stopHookActive`：Stop hook 产生 blockingError 时，下一轮重入带标志；hook 看到标志不再触发，防"模型自纠 → hook 再报错 → 再自纠"死循环。

**关键点**：职责第二次上移——s03 把安全从工具搬到权限层，s04 把权限从循环硬编码搬到 hook 扩展点；循环零改动。

**面试追问**
- log_hook 注册在 permission_hook 前面会怎样？——被拦截的调用也先打日志，日志失真；且前面的非 None 会短路掉权限检查。
- 教学版"非 None 即拦截"和 CC 的 HookResult 差在哪？——CC 有 14 个字段（permissionBehavior、updatedInput、blockingError、preventContinuation 等）；且 hook 的 allow 不能绕过 settings 的 deny/ask——策略 > hook，hook 只能更严格。

---

## s05 TodoWrite —— 没有计划，做着做着就偏了

**一句话**：先列步骤再动手 + 提醒机制拉回注意力。

**新增功能**
- `todo_write` 工具（只写计划，不做实际工作）
- todo 数据结构：`subject / status`（pending / in_progress / completed）
- todo reminder：连续 N 轮（教学 3 轮）没调 `todo_write` 就注入提醒

**解决的问题**：长任务做着做着偏离目标（改文件时被测试失败吸走注意力，忘了最初是改名）。

**实现方法**
1. 模型动手前先调 `todo_write` 把步骤落成清单。
2. 清单进入对话上下文，每轮 LLM 都能看到进度。
3. 循环里维护一个"距上次写 todo 的轮数"计数器，超阈值注入提醒。

**关键点**：计划是"外置注意力"——不依赖模型记性好，而是把计划放进它每轮必看的上下文。

**面试追问**
- 为什么 todo 要工具化而不是塞进 system prompt？——计划是动态变化的，工具可以更新状态；塞 prompt 会污染静态缓存区。
- 和 s12 Task System 的区别？——todo 是当前会话的计划清单；task 是跨会话、带依赖、多 Agent 共享的持久化任务单元。

---

## s06 Subagent —— 大任务拆小，干净上下文

**一句话**：子 Agent 独立上下文，只回传结论。

**新增功能**
- `task` 工具 → `spawn_subagent(description)`
- `SUB_SYSTEM` / `SUB_TOOLS`（bash/read_file/write_file/edit_file/glob，**没有 task**）
- `extract_text()` 摘要提取

**解决的问题**：大任务中间过程（读 30 个文件、60 轮对话）塞满上下文，Agent 记不住最初目标。

**实现方法**
1. 子 Agent 用全新 `messages = [user: description]` 跑自己的循环。
2. 工具受限（无 task）→ 不能递归 spawn，防止失控。
3. 结束后只把最终文本摘要回传主 Agent；中间过程全丢弃。
4. 文件系统副作用保留在工作目录（改的文件还在），对话上下文不保留。
5. 子 Agent 的工具调用仍走权限 hook，隔离不豁免安全。

**关键点**：上下文隔离与副作用保留的边界——"对话忘掉，文件记住"。

**面试追问**
- 为什么子 Agent 不能递归 spawn？——防止树状失控、上下文无法追踪。
- 和 s15 队友的区别？——子 Agent 一次性、只回结论；队友常驻、异步收件箱、多轮通信。

---

## s07 Skill Loading —— 用到的时候才加载

**一句话**：清单常驻，内容按需，用时才花 token。

**新增功能**
- `skills/` 目录 + skill 文件 frontmatter（name / description）
- `_scan_skills()` 启动扫描
- `load_skill` 工具：运行时读全文注入
- `build_system()`：只注入技能清单到 system

**解决的问题**：项目规范（React 规范/SQL 风格/API 文档）全塞 system prompt，占用大量常驻上下文。

**实现方法（两层设计）**
1. 启动时扫描技能目录，把"名字 + 一句话描述"清单注入 system prompt（便宜、常驻、可缓存）。
2. 模型需要时调 `load_skill(name)`，通过 **tool_result** 注入完整内容——用到才花 token。

**关键点**：技能描述要精炼（模型靠描述决定是否加载）；注入走 tool_result 而不是 system，避免污染缓存区。

**面试追问**
- 为什么技能内容不直接塞 system？——占上下文、破缓存、大部分用不到。
- 和 s09 Memory 的区别？——skill = "怎么做某事的方法"（静态知识），memory = "跨会话的事实/偏好"（动态积累）。

---

## s08 Context Compact —— 上下文总会满，要有办法腾地方

**一句话**：四层压缩，便宜的先跑、贵的后跑。

**新增功能**
- 三层零 API 预处理：`tool_result_budget` → `snip_compact` → `micro_compact`
- 一层 LLM 压缩：`compact_history`（auto compact）
- 应急路径：`reactive_compact`
- `write_transcript` 落盘审计

**解决的问题**：上下文堆满后 Agent 卡死（读了大文件 + 30 个文件 + 20 条命令输出全堆在 messages）。

**实现方法（按成本排序）**
1. `tool_result_budget`：超大输出截断 / 落盘（`persist_large` 写文件，消息里留预览）。
2. `snip_compact`：裁剪中间消息，保留头尾；**注意不拆散 tool_use/tool_result 配对**。
3. `micro_compact`：把旧的 tool_result 内容替换成占位摘要。
4. 仍超 `CONTEXT_LIMIT` → `compact_history`：LLM 总结（保留目标/发现/改动文件/剩余工作/约束 5 要素），1 次 API。
5. API 报 prompt_too_long → `reactive_compact`：只摘历史，保留最近 5 条。

**关键点**：预算截断保结构、摘要保语义；"先 0 API，后 1 API，最后应急"。

**面试追问**
- snip 时为什么不能拆散 tool_use/tool_result？——API 要求配对，拆了会协议错误。
- 压缩会丢什么？——细节（"用 tab 不用空格"→"有风格偏好"），这正是 s09 Memory 的动机。
- auto vs reactive 触发时机？——auto 是调用前预算超限；reactive 是 API 已报错后应急。

---

## s09 Memory —— 压缩会丢细节，要有一层不丢的

**一句话**：文件仓库 + 索引 + 按需加载，跨压缩、跨会话。

**新增功能（四个子系统）**
- 存储：`.memory/*.md`（YAML frontmatter：name/description/type）+ `MEMORY.md` 索引 + `_rebuild_index`
- 加载：`select_relevant_memories`（LLM side-query 选 ≤5 条，失败降级关键词）+ `load_memories`
- 写入：`extract_memories`（每轮结束，从 `pre_compress` 快照提取）
- 整理：`consolidate_memories`（文件数达阈值 → LLM 合并去重，压到 <30）
- 四类记忆：user / feedback / project / reference

**解决的问题**：压缩丢细节（偏好被简化）；新开会话连摘要都没有——LLM 无持久状态。

**实现方法**
1. 索引常驻 system prompt（便宜、可被 prompt cache 缓存）；文件内容按需注入当前 user turn（`request_messages` **拷贝**，不污染 messages）。
2. 选记忆用 LLM 而不是 embedding：记忆库规模小（≤200），LLM 能理解语义且保守（"不确定就不选"）；失败降级关键词匹配。
3. 提取用 `pre_compress` 快照：压缩有损，快照保留原始细节。
4. 防重复：提取 prompt 带上已有记忆清单，要求"已覆盖就返回 []"；防膨胀：整理压到 30 条以下 + 文件/索引硬预算。

**面试追问**
- 为什么内容注入用 request_messages 拷贝？——直接改 messages 会被压缩/写 transcript/被提取器当成对话细节重复提取（自污染）。
- User Memory 和 Session Memory 的区别？——user 跨会话、进 system；session 单会话、进 compact 摘要。`sessionMemoryCompact` 是 compact 前先看 session memory，够格（≥10K token / ≥5 条文本 / ≤40K）就不调 LLM。
- CC 的 Dream 四层门控？——时间 ≥24h、扫描节流、会话数 ≥5、文件锁。

---

## s10 System Prompt —— 运行时组装，不硬编码

**一句话**：分段定义 + 按真实状态拼接 + 确定性缓存。

**新增功能**
- `PROMPT_SECTIONS`：按主题分段的 prompt 片段
- `assemble_system_prompt(context)`：按 context 选段拼接
- `get_system_prompt(context)`：`json.dumps(sort_keys=True)` 做确定性缓存 key
- `update_context`：从真实状态推导（启用的工具 / 工作目录 / 记忆索引）

**解决的问题**：system prompt 硬编码，换项目、加工具要重写整段。

**实现方法**
1. 段分三类：静态（identity）常驻；动态（tools/workspace）来自 context；条件（memories 存在才加载）。
2. context 变化才重新组装；`json.dumps` 确定性序列化（不用 `hash()`——进程随机化 + 嵌套类型不支持）。
3. agent_loop 每轮工具执行后 `update_context` → `get_system_prompt` 重新评估。

**关键点**：教学缓存只是"进程内避免重复拼字符串"；CC 的 prompt cache 用 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 分隔静态/动态部分，静态部分命中全局 cache，不因动态内容变化失效。

**面试追问**
- 为什么用 json.dumps 不用 hash？——hash 每次进程启动随机化（PYTHONHASHSEED），且 dict/list 不可 hash。
- 工具池动态变化（s19 MCP）为什么去掉了缓存？——工具列表是 prompt 的一部分，工具池变了缓存 key 必变，缓存失去意义。

---

## s11 Error Recovery —— 错误不是结束，是重试的开始

**一句话**：按错误类型走升级阶梯，恢复后 continue 重试。

**新增功能**
- `RecoveryState`：跨循环恢复状态（是否已升级、恢复次数、连续 529 计数、是否已 reactive compact、当前模型）
- `call_model`：429/529 指数退避重试 + 切换 `FALLBACK_MODEL`
- max_tokens 升级 + continuation 续跑
- prompt_too_long → reactive compact（一次）

**解决的问题**：429/529/超时/上下文过长直接崩，Agent 没有恢复能力。

**实现方法（三条恢复路径）**
1. **max_tokens**：首次不 append 截断输出，直接提升 max_tokens（8K→64K）重试同一请求；仍截断 → 保存输出 + 注入 `CONTINUATION_PROMPT` 续跑（≤3 次）。
2. **prompt_too_long**：`reactive_compact` 一次；仍超 → 判定不可恢复，记录错误返回。
3. **429/529**：指数退避重试（`BASE_DELAY * 2^n`，上限 10 次）→ 连续 529 计数触发切换备用模型。
4. 其他异常：不可恢复，写入错误消息后返回（不无限重试）。

**关键点**：恢复是"升级阶梯"——越贵的策略越后跑；状态机防止同一错误被反复处理。

**面试追问**
- max_tokens 首次为什么先不 append 截断输出？——避免把半截内容写进上下文再重试，污染历史。
- reactive compact 为什么只做一次？——压缩有损，连续压缩会丢太多信息；一次不行说明问题不是上下文长度。

---

## s12 Task System —— 目标太大，拆成小任务

**一句话**：文件持久化的任务图，带依赖、带 owner，多 Agent 协作的基础。

**新增功能**
- `Task` dataclass：id / subject / description / status / owner / blockedBy
- `.tasks/*.json` 文件持久化
- 工具：create_task / list_tasks / get_task / claim_task / complete_task
- `can_start`：依赖检查

**解决的问题**：任务没有先后顺序，先盖屋顶再打地基（写 API 才发现没数据库表）。

**实现方法**
1. 任务落盘为独立 JSON 文件 → 重启可见、多 Agent 可见。
2. `blockedBy` 列表声明依赖；`can_start` = 所有依赖任务存在且 completed。
3. `claim_task` 检查 pending + 无 owner；`complete_task` 后下游自动可认领。

**关键点**：依赖语义是"blockedBy 中没有未完成的任务"，不是"blockedBy 为空"；教学版只演示 DAG 依赖检查，没做环检测。

**面试追问**
- 为什么文件而不是内存？——跨会话/跨 Agent 共享，任务状态要能持久。
- 环依赖会发生什么？——互相阻塞，永远无法开始；生产要做环检测（教学版省略）。

---

## s13 Background Tasks —— 慢操作放后台

**一句话**：慢命令丢后台线程，先回占位结果，完成再注入通知。

**新增功能**
- bash schema 增加 `run_in_background` 参数
- `is_slow_operation` 关键词启发式 + `should_run_background`（显式优先）
- `start_background_task`：daemon 线程 + `bg_id` + 状态字典
- `collect_background_results`：`<task_notification>` 通知

**解决的问题**：`pip install` / `npm run build` 跑几分钟，Agent 干等，LLM 空转烧 token。

**实现方法**
1. 判定：模型显式传 `run_in_background=true` 优先；没传用关键词兜底（install/build/test/deploy…）。
2. 执行：工具调用包成 worker 丢进 daemon 线程；`background_tasks` 记 running，完成后写 `background_results`，全程 `background_lock` 保护。
3. 立即回填占位 tool_result：`[Background task bg_0001 started]`——模型知道在跑，可以去干别的。
4. 完成时 `collect_background_results` 生成 `<task_notification>` 文本块，和本轮 tool_result 合并进同一条 user 消息。

**关键点**：通知**不复用 tool_use_id**——Messages API 一个 tool_use 只配一个 tool_result，后台完成是独立事件，用独立通知格式注入。

**面试追问**
- 为什么不能复用 tool_use_id 回第二个 tool_result？——违反配对协议。
- CC 的"后台"是什么机制？——Node 单线程事件循环里"不 await"；stdout/stderr 重定向文件；还有停滞看门狗（45s 无输出检测交互提示）和 7 种后台任务类型。

---

## s14 Cron Scheduler —— 按时间表生产工作

**一句话**：判定时机、缓冲、交付、消费四层解耦。

**新增功能**
- `CronJob`：id / cron / prompt / recurring / durable
- `cron_matches` 五段式匹配 + `validate_cron` 注册前校验
- `cron_scheduler_loop`：daemon 线程每秒轮询
- `cron_queue` + `queue_processor_loop` + agent_loop 消费注入
- `.scheduled_tasks.json` durable 持久化
- 工具：schedule_cron / list_crons / cancel_cron

**解决的问题**：周期任务（每天 9 点跑测试）每次都要人推。

**实现方法（四层）**
1. Scheduler：1s 轮询，`cron_matches` 命中 → 入队；`minute_marker`（"YYYY-MM-DD HH:MM"）防同分钟重复触发且跨天不误判；单 job try/except 防一个坏任务拖垮线程。
2. Queue：`cron_queue` 缓冲已触发任务，调度与执行解耦。
3. Queue Processor：0.2s 轮询，`agent_lock.acquire(blocking=False)` 判断 Agent 空闲才交付。
4. Consumer：agent_loop 开头 `consume_cron_queue`，把 job.prompt 注入为 `[Scheduled] ...` user 消息。

**关键点**：cron 语义（分钟/小时/月全匹配，DOM/DOW 同时约束取 OR；`(weekday()+1)%7` 把 Python 周一=0 转 cron 周日=0）；durable 只持久化**任务定义**——进程关了调度就停，重启恢复定义，不是"进程外常驻"。

**面试追问**
- 为什么不能调度线程直接调 agent_loop？——触发判断、并发控制、执行交付会耦合，Agent 忙时无法缓冲。
- durable 任务进程关闭后还会跑吗？——不会；要进程外运行用系统 crontab/systemd。
- CC 的差异？——抖动防惊群（重复任务延迟 ≤10%/15min）、7 天自动过期、MAX_JOBS=50、`.scheduled_tasks.lock` 防多 session 重复触发。

---

## s15 Agent Teams —— 一个搞不定，组队来

**一句话**：文件收件箱 + 队友线程 + 异步唤醒。

**新增功能**
- `MessageBus`：send（append）/ read_inbox（读+删，消费式）/ peek（非破坏检查）
- `spawn_teammate_thread`：队友独立线程 + 独立 system/messages + 简化工具集（bash/read_file/write_file/send_message）
- Lead 工具：spawn_teammate / send_message / check_inbox
- main 事件队列：input_reader + inbox_poller（1s）统一唤醒

**解决的问题**：单 Agent 注意力覆盖不了大项目；s06 子 Agent 是一次性的，没有能通信协作的队友。

**实现方法**
1. 通信：每个 Agent 一个 `.mailboxes/{agent}.jsonl`；发消息 = append 一行 JSON；读 = 读全文 + 删文件（模拟"已读游标"）。
2. 队友：daemon 线程跑自己的循环（限 10 轮），每轮开头读自己 inbox 注入 `<inbox>`；完成后 `BUS.send(name, "lead", summary, "result")`。
3. Lead：`inbox_poller` 每秒 `peek("lead")`，有货就向事件队列放 `("wake", None)`；主循环拿到 wake 读收件箱 → 注入 `[Inbox]` → 跑一轮。
4. 串行化：用户输入和 wake 都进同一个 `events` 队列，单一消费者，一次只跑一轮 agent_loop。

**关键点**：结果必须注入 history（LLM 只看得见 messages，看不见文件）；poller 用 peek 不用 read_inbox（不能消费消息）；read+unlink 有竞态（教学无锁，CC 用 proper-lockfile）。

**面试追问**
- 为什么用文件收件箱不是内存队列？——跨线程可观察、直观；CC 生产也是文件（`~/.claude/teams/.../inboxes/`）+ proper-lockfile。
- 两个来源直接跑 agent_loop 会怎样？——并发读写共享 history，上下文错乱；事件队列 = 单一消费者。
- 队友为什么限 10 轮？——防无限循环；CC 用 idle loop（发 idle_notification → 等 inbox → shutdown_request 才退出）。

---

## s16 Team Protocols —— 队友之间要有约定

**一句话**：request-response 握手，request_id 关联，类型校验。

**新增功能**
- `ProtocolState`：request_id / type / sender / target / status / payload
- `pending_requests` 字典 + `new_request_id()`
- `match_response`：request_id 关联 + 类型校验
- `dispatch_message` / `handle_inbox_message`：按消息 type 路由
- 协议消息：shutdown_request/response、plan_approval_request/response 等

**解决的问题**：松散协调——Lead 想让队友关机，直接杀线程会留下写了一半的文件；需要体面握手。

**实现方法**
1. 发起方发请求（带 request_id + 类型）。
2. 接收方按 type 路由：shutdown_request → 收尾后回 shutdown_response（approve）；plan_approval_request → 提交计划待审。
3. `match_response` 用 request_id 找到原请求，校验响应类型匹配（plan 的响应不能是 shutdown_response），更新状态。

**关键点**：request_id 防错配、类型校验防协议混乱；关机是"确认收尾"而不是"强杀"。

**面试追问**
- 为什么需要 request_id？——异步消息没有顺序保证，必须显式关联。
- CC 的完整协议有多少种消息？——15 种：idle_notification、permission_request/response、shutdown_request/approved/rejected、task_assignment、sandbox_permission_* 等。

---

## s17 Autonomous Agents —— 自己看板，自己认领

**一句话**：空闲轮询 + 任务看板 + 自动认领，不依赖 Lead 分配。

**新增功能**
- 三阶段生命周期：WORK → IDLE → SHUTDOWN
- `idle_notification` + `teammate_wake_events`（threading.Event）
- `task_watcher_loop`（.tasks/ 快照变化 + 1s debounce）+ `idle_poll`（5s 兜底）
- `scan_unclaimed_tasks` + 自动 claim + `release_task`
- 身份重注入（`len(messages) <= 3`）

**解决的问题**：看板 10 个任务，Lead 得手动 assign 10 次，不能扩展。

**实现方法**
1. WORK：内层循环（≤10 轮），处理 inbox（协议优先）+ 调 LLM + 执行工具。
2. 干完 → 进 IDLE：注册 idle_teammates、发 idle_notification、`idle_poll` 等待。
3. `idle_poll`（5s × 12 = 60s）：先读 inbox——shutdown_request 立即回复并返回 shutdown；普通消息注入返回 work。再扫任务板——`scan_unclaimed_tasks`（pending + 无 owner + can_start）→ claim 成功返回 work，失败继续扫。
4. 唤醒：task watcher 发现看板变化（新任务/依赖解锁）→ `threading.Event.set()` 立即唤醒；5s 轮询兜底。
5. 返回 timeout（60s 无活）→ SHUTDOWN：发 summary、清注册表、退出。

**关键点**：inbox 优先于任务板（协议消息比新任务紧急）；watcher 管及时、轮询管可靠；无文件锁时靠 owner 检查保证单赢家（claim 返回值要检查，"Claimed" 才注入）。

**面试追问**
- 两种唤醒源为什么不只留一个？——watcher 可能漏事件（教学版是快照比较），轮询兜底；只留轮询最坏延迟 5s。
- 身份重注入为什么看消息长度？——压缩后 messages 变短（≤3），探测到就重注入 <identity>；CC 压缩保留 system prompt，不需要。
- 和 CC 的差距？——CC 是 500ms mailbox 轮询 + 真 fs.watch + tryClaimNextTask；proper-lockfile 原子 claim；无固定超时。

---

## s18 Worktree Isolation —— 各干各的，互不干扰

**一句话**：任务管目标，worktree 管目录，按 ID 绑定。

**新增功能**
- `create_worktree`：为任务创建独立目录 + 独立分支
- `bind_task_to_worktree`：任务绑定工作目录（task.worktree 字段）
- `validate_worktree_name`（正则 `^[A-Za-z0-9._-]{1,64}$`）
- 有改动拒绝删除 + 事件日志（`.logs/*.jsonl`）
- 工具：create_worktree / remove_worktree / keep_worktree

**解决的问题**：队友同目录工作，Alice 和 Bob 都写 config.py 互相覆盖，且无法干净回滚、分不清谁的改动。

**实现方法**
1. `create_worktree` 用 git worktree 建独立工作目录和分支。
2. 任务认领时若带 worktree，工具执行 cwd 自动切换到该目录。
3. 安全：名字正则校验；目录有未提交改动时拒绝删除。
4. 事件日志记录 worktree 生命周期，审计可追溯。

**关键点**：git worktree 天然提供隔离 + 分支 + 可合并/回滚；绑定关系存在任务数据里（不是 Agent 记着）。

**面试追问**
- 为什么用 git worktree 不是普通子目录？——子目录没有独立分支，无法独立提交/回滚/合并。
- 删除保护解决什么？——防止误删带工作成果的目录。

---

## s19 MCP Plugin —— 外接工具，标准协议

**一句话**：Agent 不需要知道工具是谁写的，标准协议发现 + 调用。

**新增功能**
- `connect_mcp` 工具 + `MCPClient`（模拟 tools/list + tools/call）
- `normalize_mcp_name` 规范化命名
- `assemble_tool_pool`：动态组装 `mcp__server__tool` 工具进工具池
- 工具标注：(readOnly) / (destructive)

**解决的问题**：Jira、部署系统、Notion 等每个外部服务都要手写一套工具代码。

**实现方法**
1. `connect_mcp` 连接外部 server（教学用 mock handler 模拟；真实是子进程 + stdin/stdout JSON-RPC）。
2. 通过 `tools/list` 发现工具列表 → 规范化命名（`mcp__server__tool`，防命名空间冲突）→ 组装进工具池。
3. 模型调用时走 `tools/call` 转发到外部 server。
4. 工具池动态变化 → 去掉 prompt 缓存（缓存 key 必变，缓存失去意义）。

**关键点**：MCP 把"工具发现/调用"标准化，harness 只认协议不认实现。

**面试追问**
- MCP 核心概念？——tools/list（发现）、tools/call（调用）、JSON-RPC over stdio。
- 为什么去掉了 s10 的 prompt 缓存？——工具池是 prompt 的一部分，MCP 连接后工具动态增减，缓存无法命中。

---

## s20 Comprehensive —— 机制很多，循环一个

**一句话**：前 19 章机制全部挂到同一个 while True 上。

**新增功能**
- 全量合并：内置 + MCP 工具池、PreToolUse 权限、四 hooks、todo、skill、compact、error recovery、后台任务、团队、worktree

**解决的问题**：真实长期运行的 coding agent 需要同时拥有所有机制，而不是每章只带一个。

**实现方法**
1. 循环不变：消费输入 → 组装 system → 调 LLM → 处理 tool_use → 回填 → 压缩/记忆/后台收尾。
2. 每个机制通过注册表/挂载点/事件接入：工具走 dispatch map，扩展走 hooks，异步走事件队列，外部能力走 MCP。
3. 分层：loop 内 = 对话推进；loop 外 = 调度（cron）、唤醒（poller/queue processor）、持久化（任务/记忆/worktree）。

**关键点**：架构稳定的根本是"核心循环 + 扩展点"——新增能力不改循环，只加注册项。

**面试追问**
- 从零设计一个 coding agent，你怎么分层？——核心循环（对话推进）→ 安全（权限/hooks）→ 状态（记忆/任务/压缩）→ 执行（工具/后台/团队）→ 扩展（MCP/技能）。
- 哪些机制必须放 loop 内、哪些放 loop 外？——对话相关的（权限、工具分发、压缩）在 loop 内；时间/异步事件（cron、后台、队友消息）在 loop 外通过队列/事件注入。

---

## 面试速记表

| 面试官问 | 你要答 |
|----------|--------|
| Agent 怎么循环 | stop_reason == tool_use → 执行 → tool_result 回填 → 继续；否则退出 |
| 加工具要改几处 | schema + handler 两处，循环不动 |
| 权限怎么防 rm -rf | 三道闸门：deny 先、ask 中、放行兜底；数据驱动规则表 |
| hooks 怎么不破坏循环 | 注册表 + trigger_hooks 短路；None=放行、非None=拦截/续跑 |
| 上下文满了怎么办 | 预算截断 → snip → micro（0 API）→ LLM 摘要（1 API）→ reactive |
| 跨会话记忆 | 文件 + 索引常驻 system + LLM 按需选 + 每轮结束提取 + 阈值合并 |
| 慢命令 | run_in_background → daemon 线程 → 占位 tool_result → 完成注入通知 |
| 定时任务 | 调度线程判定 → 队列缓冲 → 空闲交付 → 主循环消费（四层解耦） |
| 多 Agent 通信 | 文件收件箱 + 消费式读取 + poller 唤醒 + 事件队列串行化 |
| 队友自治 | WORK→IDLE→SHUTDOWN；空闲扫看板自动认领；watcher 及时 + 轮询可靠 |
| 防互相覆盖 | git worktree：每任务独立目录+分支，按任务 ID 绑定 |
| 接外部服务 | MCP：tools/list 发现 → tools/call 调用 → 动态工具池 |
