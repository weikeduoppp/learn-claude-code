# s01–s07 复习资料

> 主线:从"一个循环"到"会计划、会拆活、会用知识"。
> Harness 视角:模型提供智能,代码提供环境。每一章给 Agent 加一个能力,循环本体几乎不动。

配套材料:各章 `README.md` + `code.py`;自己的笔记:[s03 学习记录](s03_permission/学习记录.md)、[s04 学习](s04_hooks/学习.md)。

## 0. 怎么用

1. 先看"一页总览",回忆每章讲什么;
2. 逐章过"要点",对照自己的笔记查漏;
3. 做最后的"自测题",答不上来的回到对应章节。

## 1. 一页总览

| 章节 | 一句话 | 新增机制 | 新增工具/函数 | 循环本体变化 |
|---|---|---|---|---|
| s01 Agent Loop | 一个循环 + 一个 bash 工具 = 最小 Agent | agent loop | bash | 循环诞生 |
| s02 Tool Use | 加一个工具 = 加一个 handler | 分发映射 TOOL_HANDLERS + safe_path | read/write/edit/glob | 无 |
| s03 Permission | 执行前装三道门 | 三道闸门管线 | check_deny_list / check_rules / ask_user / check_permission | +1 行 |
| s04 Hooks | 扩展挂钩子,不写进循环 | HOOKS 注册表 | register_hook / trigger_hooks | 换成 trigger_hooks |
| s05 TodoWrite | 先计划再动手 | todo_write + nag reminder | todo_write | +计数器 |
| s06 Subagent | 大任务拆小,干净上下文 | 子 Agent(独立 messages[]) | task → spawn_subagent | 无(新增子循环) |
| s07 Skill | 用到才加载知识 | 两级技能加载 | load_skill | 无 |

## 2. 逐章要点

### s01 Agent Loop —— 一切的起点

**问题**:让模型能"干活"。**方案**:最小循环。

```python
while stop_reason == "tool_use":
    response = LLM(messages, tools)
    execute tools
    append results to messages
```

要点:

- 模型决定何时调工具、何时停;代码只负责执行模型的要求。
- tool_result 必须回填,否则模型看不到结果、无法继续。
- 唯一工具 bash,内含危险命令黑名单、120s 超时、50k 输出截断——注意这是"工具内部校验",它的位置在 s03 会被改变。
- 心智模型:agent = 模型 + harness;harness 从这一章开始长出来。

### s02 Tool Use —— 工具系统

**问题**:一个 bash 不够。**方案**:工具数组 + 分发映射。

```python
TOOLS = [ {"name": "bash", ...}, ... ]      # 告诉模型有哪些工具
TOOL_HANDLERS = {"bash": run_bash, ...}     # 告诉代码怎么执行
```

要点:

- 加一个工具 = 改两处(TOOLS + TOOL_HANDLERS),循环不用动。这个"两处接入"模式后面每章都在用。
- safe_path:file 工具内部校验路径不逃出工作区——安全职责在工具内部(会被 s03 上移)。
- 分发机制让循环不关心具体工具,这是循环能保持稳定的前提。

### s03 Permission —— 执行前的门

**问题**:s02 的 bash 裸奔。**方案**:三道闸门。

```
Gate1 DENY_LIST(硬拒绝) → Gate2 PERMISSION_RULES(规则命中) → Gate3 ask_user(询问) → 通过才执行
```

要点:

- 顺序理由:便宜/绝对的检查在前(fail-fast),打断人的用户审批放最后。
- 拒绝后:循环把 "Permission denied." 作为 tool_result 回填,handler 不执行,模型据此调整策略。
- 规则表是数据(PERMISSION_RULES 列表),加规则 = 加一行 dict,不改逻辑——数据驱动的第一次出现。
- safe_path 被删掉:安全职责从工具内部上移到权限层,拦截点变了、保证不变。
- 教学简化:字符串匹配不是可靠安全机制(`rm -fr /`、`rm --recursive --force /`、`find / -delete` 都能绕过),CC 用 AST 解析 + 多阶段管线 + 分类器(见 s03 README 附录)。

### s04 Hooks —— 扩展挂载点

**问题**:每加一个检查都要改循环。**方案**:注册表 + 触发。

```python
HOOKS = {"UserPromptSubmit": [], "PreToolUse": [], "PostToolUse": [], "Stop": []}
def register_hook(event, callback): HOOKS[event].append(callback)
def trigger_hooks(event, *args): ...   # 返回第一个非 None(短路版)
```

四个事件时机:输入进 LLM 前 / 工具执行前 / 工具执行后 / 循环退出前——覆盖一个 query 的完整生命周期。

返回值约定:

- None = 放行/继续;
- PreToolUse 非 None = 拦截,值作为 tool_result 回填;
- Stop 非 None = **强制续跑**(值注入为 user 消息,循环 continue);summary_hook 打印总结是副作用,不是非 None 的含义。

要点:

- 注册顺序 = 执行顺序;permission_hook 必须在 log_hook 前,否则被拦的调用也会被记录。
- 职责上移的第二次:s02 安全在工具内 → s03 在权限层 → s04 在 hook 扩展点,循环只认识 trigger_hooks。
- CC 对照:27 个事件;HookResult 14 字段;关键不变式"hook allow 不能绕过 settings deny/ask"(策略 > hook);stopHookActive 防无限自纠循环(你已在 s04 实现);hook_stopped_continuation 优雅停机。

### s05 TodoWrite —— 规划

**问题**:多步任务会跑偏。**方案**:todo_write 工具(带状态的列表)+ nag reminder。

要点:

- todo_write 不增加任何执行能力,增加的是规划能力——"外部记忆"对抗上下文稀释。
- 状态机:pending → in_progress → completed;每次调用传整个列表(全量替换)。
- nag reminder:连续 3 轮没调 todo_write,循环在 LLM 调用前注入 `<reminder>`;调 todo_write 即重置计数。
- SYSTEM 提示加入"先计划再执行"。
- CC 对照:TodoWrite V1(内存)vs Task System V2(s12,文件持久化 + 依赖图);CC 无固定 3 轮逻辑,而是 3 个以上 todo 全完成且缺 verification 时 nudge。

### s06 Subagent —— 拆任务

**问题**:一个对话装不下大任务。**方案**:task 工具 → spawn_subagent。

关键设计决策:

| 决策 | 选择 | 原因 |
|---|---|---|
| 上下文隔离 | 全新 messages[] | 中间过程不污染主对话 |
| 只回传结论 | extract_text(最后文本) | 不是回传整个历史 |
| 禁止递归 | 子 Agent 无 task 工具 | 防止无限嵌套 |
| 安全上限 | 30 轮 | 防失控 |
| 安全不跳过 | 子 Agent 工具也走 PreToolUse hook | 上下文隔离 ≠ 权限豁免 |

要点:

- 文件系统副作用(写文件/改文件)保留在工作目录,只有对话上下文被丢弃。
- 子 Agent 有独立 SUB_SYSTEM("直接完成,不要再委派")。
- CC 对照:子 Agent 的权限模式是 bubble(弹窗冒泡到父终端);真实 CC 还有 plan mode 等机制。

### s07 Skill Loading —— 按需知识

**问题**:把规范全塞 system prompt 浪费 token。**方案**:两级加载。

| 层 | 位置 | 时机 | 代价 |
|---|---|---|---|
| 目录(catalog) | SYSTEM prompt | 启动扫描 skills/,解析 frontmatter(name/description) | ~100 tokens/技能,每轮都带 |
| 内容(content) | tool_result | 模型调 load_skill 时 | ~2000 tokens,按需 |

```python
SKILL_REGISTRY[name] = {"name":..., "description":..., "content": raw}  # 启动时填充
def load_skill(name): return SKILL_REGISTRY.get(name, ...)              # 走注册表,不走路径
```

要点:

- load_skill 走注册表查找 = 防路径遍历(不能传 `../../etc/passwd`)。
- SKILL.md 加载后可作为指引,后续用 file/bash 访问 references/、scripts/、assets/。
- 子 Agent 的 SUB_SYSTEM 不注入技能目录。
- 与 s08 衔接:按需加载解决"不该带的别带",compact 解决"该丢的怎么丢"。
- CC 对照:技能来源远不止一个目录(user/project/--add-dir/legacy commands/bundled/MCP);frontmatter 字段更多(when_to_use、allowed-tools、context: inline/fork、model、hooks、paths 条件激活等);catalog 预算约上下文 1%(上限 8000 字符)。

## 3. 贯穿主线(跨章节的"肌肉记忆")

1. **职责上移**:安全从工具内(s02 safe_path)→ 权限层(s03)→ hook 扩展点(s04)。每次上移,核心更干净。
2. **数据驱动**:PERMISSION_RULES(规则)、HOOKS(回调表)、SKILL_REGISTRY(技能表)——"加东西" = 加数据/注册,不改逻辑。
3. **循环稳定**:新机制一律通过"新工具 + 分发"或"新 hook + 注册"接入,agent_loop 本体几乎不变。写 harness 时记住:循环是稳定的核心。
4. **上下文管理三步曲**:s06 隔离(拆出去)、s07 按需加载(少带)、s08 压缩(丢掉)——都在对抗同一个敌人:上下文稀释。
5. **安全贯穿**:权限 hook 在主 Agent 和子 Agent 都生效,隔离上下文不等于豁免权限。

## 4. 自测题

先自答,再看答案(答案在对应章节要点里,答不上来就回去翻):

1. agent loop 什么时候停止?为什么 tool_result 必须回填?
2. 加一个新工具,需要改哪几处?s02 之后循环为什么不用动?
3. s03 三道闸门的顺序和理由?拒绝后循环做什么?
4. 为什么 s03 能删掉 safe_path?(职责上移)
5. s04 四个 hook 事件的时机?Stop 的非 None 返回值意味着什么?
6. 同一事件挂多个 hook,顺序由什么决定?log 在 permission 前面会怎样?
7. 为什么 CC 坚持 hook allow 不能绕过 deny/ask?
8. todo_write 给 Agent 增加的是什么能力?nag reminder 的触发条件?
9. 子 Agent 的上下文隔离怎么实现?为什么子 Agent 没有 task 工具?
10. s07 的两级加载分别在什么时候、花多少 token?为什么 load_skill 不走文件路径?

## 5. 易错点清单

- Stop 非 None = **强制续跑**,不是"总结"(总结是 summary_hook 的副作用)。
- trigger_hooks 两种语义:短路(第一个非 None 生效)vs 全部执行(最后一个非 None 生效)——先确认自己代码是哪种。
- s03 deny list 是子串匹配:`rm -rf /; echo hi` 反而会被拦住;`rm -fr /`、`find / -delete` 这类变体才绕得过。
- todo_write 是**全量替换**:每次调用要传整个列表。
- 子 Agent 只回传**摘要文本**,不是 messages 列表。
- s07 注入 SYSTEM 的是"目录"(名字+描述),完整内容要 load_skill 才进上下文。
- nag reminder 按"轮数"计,不是按时间——LLM 没有时钟。

## 6. 下一步预告

- s08 Context Compact:该丢的怎么丢(四层压缩,便宜的先跑)——和 s07 是连续的两半。
- s09 Memory:记住该记的、忘掉该忘的。
- s12 Task System:todo_write 的升级版(文件持久化 + 依赖图 + 并发锁)。
- s15+ 团队:s06 单层子 Agent 扩展成多 Agent 协作(MessageBus、协议、worktree 隔离)。
