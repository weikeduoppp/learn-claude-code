学习地图
先看问题定义，确认这一章到底在对抗什么。
读 [README.md (line 12)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:12) 到 [README.md (line 26)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:26)。
你要先抓住这一章的敌人不是“没有知识”，而是 把所有知识都提前塞进 system prompt。
核心问题：大部分文档在当前任务里根本用不上，但每轮请求都要带着，浪费 token，还污染上下文。

看解决方案，先记住“两层加载”。
读 [README.md (line 29)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:29) 到 [README.md (line 42)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:42)。
这一章最重要的结构就是两层：
第 1 层：技能目录，便宜，常驻在 SYSTEM 里
第 2 层：技能全文，贵，只有调用 load_skill 时才进入当前对话

把这句话背下来就够了：
先让模型知道“有什么技能”，再让模型按需取“技能全文”。
先看启动期逻辑，不要先看工具。
代码里先读 [code.py (line 54)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:54) 到 [code.py (line 104)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:104)。
这里是本章最核心的启动期知识注入流程：_parse_frontmatter()：解析 SKILL.md 的 frontmatter
_scan_skills()：扫描 skills/ 目录，建立 SKILL_REGISTRY
list_skills()：只生成名称和描述
build_system()：把技能目录注入 SYSTEM

你要看明白一点：
SYSTEM 里放的不是完整技能内容，而只是技能目录。
再看 load_skill()，理解运行期加载。
读 [code.py (line 267)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:267) 到 [code.py (line 276)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:276)。
这段代码很短，但意义很大：它不是直接让模型读文件路径
它是通过 SKILL_REGISTRY 查名字
找到后返回完整 SKILL.md

这里要记住一句：
技能加载走注册表，不走任意路径。
这既是按需加载，也是安全边界，避免路径遍历。
最后再看工具注册，确认 loop 依然没变。
读 [code.py (line 283)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:283) 到 [code.py (line 307)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/code.py:307)。
这里最该看到的是：load_skill 只是多了一个工具
TOOL_HANDLERS["load_skill"] = load_skill
没有新调度框架

所以本章再次在证明同一个原则：
新增能力优先走普通 tool protocol，不改主 loop。
把本章和 s08 连起来理解。
读 [README.md (line 103)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:103) 到 [/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md (line 103)](/Users/bytedance/Downloads/te/learn-claude-code/s07_skill_loading/README.md:103)。
这里是章节衔接的关键：s07 解决“不要提前带不该带的知识”
s08 解决“已经带进来的旧内容怎么压缩和丢掉”


学完 s07 你应该能说出的 4 句话
s07 解决的是知识注入成本，不是任务拆分问题。
技能分两层：目录常驻，全文按需加载。
load_skill 仍然是普通工具，不需要新调度机制。
技能全文进入的是当前对话历史，不是永久塞进 system prompt。




s07 相比 s06，新增的核心能力是什么？它解决的具体问题是什么？ 新增知识skill目录. 按需加载. 解决的具体问题是知识注入成本, 大部分文档在当前任务里根本用不上，但每轮请求SYSTEM都要带着，浪费 token，还污染上下文。

s07 为什么不把所有 SKILL.md 全文直接塞进 SYSTEM prompt？ 大部分文档在当前任务里根本用不上，但每轮请求SYSTEM都要带着，浪费 token，还污染上下文。system prompt 应该承载“稳定、通用、低成本”的信息，不应该承载大量高成本、低相关性的长文档。

s07 里的“两层加载”分别是什么？它们各自放在哪里、起什么作用？ 目录层在system, 按需加载层就是skill 全文, 调用load skill 加载skill 全文

load_skill 为什么仍然是普通工具，而不是一个专门的知识子系统入口？ load_skill只是工具, 证明同一个原则：新增能力优先走普通 tool protocol，不改主 loop。

SKILL_REGISTRY 的作用是什么？为什么 load_skill 要通过注册表按名字查，而不是直接让模型传文件路径？ SKILL_REGISTRY skill全文仓库. 对应获取skill 全文, 安全边界，避免路径遍历。模型不能借 load_skill 这个工具随便指定文件路径去读磁盘上的任意内容, load_skill 只允许访问“预先登记过的技能对象”，不允许访问“模型临时指定的任意文件路径”。


s07 里，为什么技能目录适合放进 SYSTEM，但技能全文不适合？  system prompt 应该承载“稳定、通用、低成本”的信息，不应该承载大量高成本、低相关性的长文档。知道有什么技能就够了, 需要再去加载

load_skill 返回的 skill 全文，为什么是进入当前 messages，而不是回写到 SYSTEM？ 加载当前会话里就够了, 加在SYSTEM的话,换个话题skill 全文还在上下文里. 不相关. 污染上下文且占用

s07 的收益是“让模型更懂领域知识”，还是“让知识注入更经济”？为什么？让知识注入更经济 上下文空间很贵
s07 优化的是知识分发成本，不是模型推理能力本身

s07 为什么会自然通向 s08？光有按需加载还不够解决什么问题？上下文还是会满的.上下文空间不足的问题. s08 压缩上下文


SKILL_REGISTRY 在 s07 里除了安全，还有一个架构上的意义，是什么？不知道
SKILL_REGISTRY 除了安全边界，还有一个很重要的架构意义：它把“技能发现”与“技能使用”分开了。

为什么说 s07 继续证明了 “loop unchanged”？只是新增了TOOL_HANDLERS

用你自己的话总结一句：s07 的本质到底是什么？不要说“skill loading”，说设计本质。s07 优化的是知识分发成本，不是模型推理能力本身