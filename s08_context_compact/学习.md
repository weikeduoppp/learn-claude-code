# s08 学习地图:Context Compact

## 1. 这一章在对抗什么

Agent 跑着跑着不动了——不是能力问题,是上下文满了。每条命令输出、每个文件内容全堆在 `messages` 里,上下文窗口有限,满了之后 API 直接拒绝:`prompt_too_long`。

解决方案:在每轮 LLM 调用前插入四层压缩管线。核心设计一句话:**便宜的先跑,贵的后跑**。

## 2. 前置检查

- s01:agent_loop 结构(tool_use block、tool_result 回填)
- s04:hooks 的 PreToolUse/PostToolUse
- s06:subagent(注意 s08 的 subagent 不继承压缩)
- s07:skill 加载、build_system

s08 骨架全部继承 s07,只新增 `compact` 工具(第 9 个)。子代理的 `SUB_TOOLS` 里没有 compact/todo_write/load_skill——子代理短命,不做压缩。

## 3. 核心设计:四层管线 + 应急 + compact 工具

| 层 | 函数 | 解决什么 | 成本 |
|---|---|---|---|
| L1 | [snip_compact](D:/code/ss/learn-claude-code/s08_context_compact/code.py:297) | 消息条数太多(>50 条):保头 3 + 尾 47,中间裁掉,留一行 `[snipped N messages]` | 0 API |
| L2 | [micro_compact](D:/code/ss/learn-claude-code/s08_context_compact/code.py:324) | 旧 tool_result 内容累积:保留最近 3 条完整,更旧的超 120 字符换一行占位符 | 0 API |
| L3 | [tool_result_budget](D:/code/ss/learn-claude-code/s08_context_compact/code.py:341) | 单批结果太大(最后一条 user 消息 tool_result 合计 >200KB):从最大的开始落盘 `.task_outputs/tool-results/`,上下文留 `<persisted-output>` 标记 + 前 2000 字符预览 | 0 API |
| L4 | [compact_history](D:/code/ss/learn-claude-code/s08_context_compact/code.py:377) | 前三层不懂内容,字符估算仍超 `CONTEXT_LIMIT=50000`:存 transcript → LLM 摘要 → 全部换一条摘要 | 1 API |
| 应急 | [reactive_compact](D:/code/ss/learn-claude-code/s08_context_compact/code.py:385) | API 仍返回 prompt_too_long:只总结较早历史,保留最近约 5 条原始消息;重试上限 1 次 | 1 API |

关键常量([code.py:267](D:/code/ss/learn-claude-code/s08_context_compact/code.py:267)):

```python
CONTEXT_LIMIT = 50000      # 字符数估算阈值(教学版用 len(str(messages)),不是精确 token)
KEEP_RECENT = 3            # L2 保留最近几条完整 tool_result
PERSIST_THRESHOLD = 30000  # L3 单条超过 30K 字符才落盘
MAX_REACTIVE_RETRIES = 1   # reactive 重试上限
```

## 4. 阅读路线

第一步:README 全文——[s08_context_compact/README.md](D:/code/ss/learn-claude-code/s08_context_compact/README.md),配合 `images/compaction-layers.svg` 图。

第二步:code.py 按这个顺序读:

| 顺序 | 位置 | 看什么 |
|---|---|---|
| 1 | [code.py:267](D:/code/ss/learn-claude-code/s08_context_compact/code.py:267) | 常量:CONTEXT_LIMIT / KEEP_RECENT / PERSIST_THRESHOLD |
| 2 | [code.py:278](D:/code/ss/learn-claude-code/s08_context_compact/code.py:278) 附近 | `_message_has_tool_use` / `_is_tool_result_message`——配对保护的依据 |
| 3 | [code.py:297](D:/code/ss/learn-claude-code/s08_context_compact/code.py:297) | L1 snip:头尾两个切口的配对保护 |
| 4 | [code.py:324](D:/code/ss/learn-claude-code/s08_context_compact/code.py:324) | L2 micro:按位置保留最近 3 条 |
| 5 | [code.py:341](D:/code/ss/learn-claude-code/s08_context_compact/code.py:341) | L3 budget:排序、落盘、预览;单条 ≤30K 不落盘 |
| 6 | [code.py:359](D:/code/ss/learn-claude-code/s08_context_compact/code.py:359) | transcript + summarize + compact_history |
| 7 | [code.py:385](D:/code/ss/learn-claude-code/s08_context_compact/code.py:385) | reactive:保留尾部 5 条 + 配对保护 |
| 8 | [code.py:456](D:/code/ss/learn-claude-code/s08_context_compact/code.py:456) | agent_loop:预处理器在哪跑、auto 在哪触发、compact 工具怎么特殊处理 |

第三步:重点看 agent_loop 里 compact 工具的分支([code.py:490](D:/code/ss/learn-claude-code/s08_context_compact/code.py:490))——它不走 TOOL_HANDLERS,也不走 PreToolUse hook,是 loop 里的特判:执行 compact_history → 回填 `[Compacted...]` → break 结束当前轮。

## 5. 必须理解透的设计决策

**顺序不能换。** L3(budget)在 L2(micro)前面:micro 会把旧的大 tool_result 替换成一行占位符,完整内容就没了;budget 必须在此之前把内容落盘。CC 源码同样把 `applyToolResultBudget` 放最前。

**配对保护。** 所有切消息的地方(包括 reactive)都要保证 `assistant(tool_use)` 和后面的 `user(tool_result)` 不被拆开——API 硬性要求,拆开就报错。

**压缩前先落盘。** `write_transcript` 把完整对话写成 JSONL 到 `.transcripts/`;压缩是破坏性操作,transcript 保留可恢复记录。但教学版没有检索工具,模型上下文里只剩摘要。

**防失控。** auto 摘要熔断(连续失败 3 次停,README 设计,CC 里是 `MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3`);reactive 重试上限 1 次,超过抛异常交给 s11;API 调用成功后 `reactive_retries` 清零。

**教学版简化(刻意)。** token 用字符数估算;micro 用文本占位而不是 API 层 `cache_edits`;没有 readFileState、没有压缩后恢复、没有 contextCollapse/sessionMemoryCompact。

## 6. 动手实验

```sh
# Windows PowerShell
cd D:\code\ss\learn-claude-code
python s08_context_compact\code.py

# 或 WSL
wsl bash -lc "cd /mnt/d/code/ss/learn-claude-code && .venv/bin/python s08_context_compact/code.py"
```

试 README 的三个 prompt:

| prompt | 预期观察 |
|---|---|
| 连续读多个文件(README.md → code.py → s01 README) | L2:旧 tool_result 被占位 |
| `Read every file in s08_context_compact/` | L3:大结果落盘到 `.task_outputs/tool-results/`,上下文只留标记+预览 |
| 反复对话 20+ 轮 | `[auto compact]` 或 `[reactive compact]` 出现;`.transcripts/` 落盘 |

观察重点:每次工具执行后旧 tool_result 是否被压缩;token 超阈值时是否自动摘要;模型会不会主动调 compact 工具。

## 7. 进阶对照(README 附录)

- 执行顺序:教学版 budget → snip → micro → auto;CC 是 budget → snip → micro → **collapse** → auto(`query.ts:379-468`),教学版没有 contextCollapse。
- snip:教学版按条数(头 3 + 尾 47);CC 仅主线程启用,`snipCompactIfNeeded` 返回 `{ messages, tokensFreed, boundaryMessage? }`,还暴露 `SnipTool` 让模型主动调。
- micro:教学版按位置保留最近 3 条;CC 有 time-based(60 分钟)和 cached(按计数)两条路径,legacy path 已移除。
- 压缩后恢复:教学版只留摘要;CC 按预算重新附加最近文件(最多 5 个 × 5K token,总 50K)加计划/agent/skill/tool 上下文。
- 阈值:教学版字符估算 50K;CC 精确 token:`contextWindow - maxOutputTokens - 13_000`。
- 摘要要求:教学版 5 类信息;CC 9 个部分 + `<analysis>`/`<summary>` 双标签,且首尾双重禁止调工具。
- contextCollapse:独立上下文管理系统,启用时抑制 proactive autocompact;manual `/compact` 和 reactive fallback 不受影响。
- sessionMemoryCompact:compact_history 前先用 s09 的 session memory 做轻量摘要、不调 LLM——学完 s09 回来再看。

## 8. 验收标准(学完自测)

Q1: 四层压缩分别解决什么问题?为什么说"便宜的先跑,贵的后跑"?

A: L1 裁旧消息、L2 旧结果占位、L3 大结果落盘都是纯结构操作,0 API;L4 才调 LLM 摘要(1 API);reactive 是 API 报错后的最后兜底。成本从低到高排列,触发从主动到被动。

Q2: 执行顺序为什么是 budget → snip → micro,不能换?

A: micro 会把旧的大 tool_result 替换成一行占位符,完整内容就没了;budget 必须在此之前把内容落盘,否则落盘逻辑永远拿不到完整内容。CC 源码同样把 `applyToolResultBudget` 放最前。

Q3: snip_compact 和 reactive_compact 为什么都要检查 tool_use/tool_result 配对?

A: API 要求 tool_use 之后必须紧跟对应 tool_result;切口若落在二者之间会产生孤立块,API 报错。snip 在头尾两个切口都做保护,reactive 切尾部 5 条时做同样的检查。

Q4: compact_history 和 reactive_compact 有什么区别?

A: 触发方式:auto 主动(每轮前字符估算超 50K,或模型调 compact 工具),reactive 被动(API 报 prompt_too_long 后)。压缩策略:auto 把全部消息换成一条摘要(可能丢最近细节),reactive 只总结较早历史、保留最近约 5 条原始消息,更温和。重试:auto 熔断 3 次(CC 的 `MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES`),reactive 上限 1 次,超过抛异常。

Q5: 为什么压缩前要写 transcript?教学版和 CC 在"压缩后恢复"上差在哪?

A: 压缩是破坏性操作,transcript(JSONL)保留可恢复的完整记录,但教学版没有检索工具,模型上下文里只剩摘要。CC 压缩后会按预算重新附加最近文件(5 个 × 5K token,总 50K)以及计划、agent/skill/tool 上下文。

Q6: compact 工具是干什么的?模型主动调用时 agent_loop 做了什么?

A: 它是 s08 新增的第 9 个工具,让模型在上下文紧张时主动请求压缩。agent_loop 检测到 `block.name == "compact"` 就执行 compact_history,回填 tool_result 为 `[Compacted...]` 并 append,然后 break 结束当前轮,用压缩后的上下文开始新一轮。

Q7: 熔断器和重试上限防什么?

A: 防"压缩失败 → 重试 → 再失败"死循环烧 API。reactive 超上限直接抛异常,完整错误恢复留给 s11;API 调用成功后重试计数清零,说明系统恢复健康。

**速记:** 四层 = 裁中间 → 旧结果占位 → 大结果落盘 → LLM 摘要;顺序 budget→snip→micro 不能换;配对保护防孤立 tool_result;transcript 保证可恢复;auto 熔断 3 次、reactive 重试 1 次;compact 工具让模型主动压缩。
