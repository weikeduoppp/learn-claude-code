# 学习地图

## s18 到 s19

`s18` 解决的是“多个 Agent 在哪里工作互不干扰”。

`s19` 解决的是“Agent 的工具能力为什么必须都手写，外部服务怎么标准接入”。

一句话记住：

> `s18` 管目录隔离，`s19` 管能力接入。

再压成一句：

> 从“在哪干活”，进入“拿什么工具干活”。

---

## 本章解决什么问题

这章的敌人不是“工具不够多”，而是：

- 所有工具都要手写，扩展成本高
- 外部系统接入没有统一协议
- 多个外部服务容易发生工具名冲突
- 工具池不再是静态常量，而会在运行时变化

所以 `s19` 解决的不是“再多加几个工具函数”，而是：

> 把外部能力通过标准协议接进 Agent，让工具池动态扩展。

---

## 相比 s18 新增了什么

相对 `s18`，这章新增的是**MCP 工具发现与动态组装**。

核心新增：

- `MCPClient`，见 [code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:662)
- `connect_mcp()`，见 [code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:741)
- `normalize_mcp_name()` 相关命名规范化逻辑
- `assemble_tool_pool()`，见 [code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:756)
- `mcp__server__tool` 工具命名空间
- 动态工具池替代固定 `BUILTIN_TOOLS`

一句话：

> `s19` 新增的不是“某个工具”，而是“工具发现和接入协议”。

---

## 核心设计

### 1. MCP 解决的是扩展接口，不是具体业务

这一章最重要的抽象是：

> Agent 不需要知道工具是谁写的，只需要知道如何发现和调用。

所以 MCP 的本质不是“docs server”或“deploy server”，而是统一外部能力接入方式。

### 2. `connect_mcp()` 只负责连接和发现

`connect_mcp()` 做的不是执行业务，而是：

- 连接 server
- 拿到工具定义
- 把 client 放进当前连接表

这说明接入层和工具执行层已经分开了。

### 3. 工具池必须动态组装

`assemble_tool_pool()` 的核心意义是：

> 每轮都根据当前已连接 MCP server，重新拼出真实可用的工具集合。

这和前面固定工具列表的章节不同，因为工具池现在是会变的。

### 4. `mcp__server__tool` 是命名冲突隔离层

如果不同 server 都有 `search` 工具，直接平铺就会冲突。  
所以需要前缀命名空间：

- `mcp__docs__search`
- `mcp__deploy__search`

这说明 `s19` 除了接入能力，还解决了：

> 多服务同名工具的隔离问题。

### 5. 规范化名字是安全边界的一部分

`normalize_mcp_name()` 不是为了好看，而是为了：

- 防特殊字符
- 防命名注入
- 保持工具名稳定可引用

所以本章除了扩展性，也很强调接入边界安全。

### 6. 动态工具池会破坏 prompt cache 的前提

之前很多章节都能缓存 system prompt。  
到了 `s19`，连接新的 MCP server 之后，工具池变了，prompt 也必须跟着变。

这说明：

> prompt cache 依赖“工具集合稳定”，而 MCP 打破了这个前提。

所以教学版在这里去掉缓存，是为了保持工具池和 prompt 同步。

---

## 建议阅读顺序

### 1. 先看问题定义

阅读：
[README.md](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/README.md:13)
到
[README.md](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/README.md:21)

### 2. 再看方案总览

阅读：
[README.md](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/README.md:23)
到
[README.md](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/README.md:39)

### 3. 先看 `MCPClient`

阅读：
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:662)
到
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:740)

### 4. 再看 `connect_mcp()`

阅读：
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:741)
到
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:755)

### 5. 再看 `assemble_tool_pool()`

阅读：
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:756)
到
该函数结束。

### 6. 最后看 `agent_loop()`

阅读：
[code.py](/Users/bytedance/Downloads/te/learn-claude-code/s19_mcp_plugin/code.py:964)
到文件结尾。

重点看每轮如何重组工具池和 system prompt。

---

## 为什么这样设计

### 为什么不直接把外部工具硬编码进代码？

因为那样每接一个新系统都要：

- 写新 schema
- 写新 handler
- 改主工具池

这违背了插件化初衷。

### 为什么一定要有 `assemble_tool_pool()`？

因为现在工具来源不止一个：

- 内置工具
- 外部 MCP 工具

所以系统需要一个统一汇总入口，把它们拼成“当前这一轮的真实工具池”。

### 为什么 MCP 工具名必须带 server 前缀？

因为插件化之后，工具重名是常态，不是边缘情况。  
前缀命名是 namespace 机制，不是装饰。

### 为什么只有 Lead 用 MCP 工具？

这是教学简化。  
这章主要先教“外部能力如何接入主工具池”，而不是把整个团队权限传播和配置继承也一起展开。

---

## 关键知识点

1. `s19` 升级的是能力接入模型，不是目录模型。
2. MCP 的核心是“发现 + 调用”协议，不是某个具体 server。
3. `connect_mcp()` 负责连接和发现，`assemble_tool_pool()` 负责组装可用工具池。
4. `mcp__server__tool` 解决多服务工具重名问题。
5. 动态工具池意味着 system prompt 也必须动态更新。
6. 名称规范化是接入安全边界的一部分。

---

## 和前后章节的关系

### 相对 `s18`

`s18` 解决“在哪工作”。  
`s19` 解决“有哪些外部能力可以用”。

### 为什么自然通向 `s20`

到了这里，系统已经有：

- 任务
- 协作
- worktree
- cron
- MCP

下一步不再是新机制，而是：

> 这些机制怎么重新放回一个完整 harness 里。

这正是 `s20`。

---

## 扩展点与局限

### 教学版的扩展点

- 支持真实 stdio/server 通信
- 把 MCP 工具开放给队友
- 引入权限系统和 annotation 结构
- 加强连接失败与断线恢复

### 教学版的局限

- 用 mock server 代替真实 transport
- 只演示最小发现/调用模型
- 未展开复杂权限和认证
- 未覆盖多 transport 差异

---

## 三轮问答

### 第一轮

1. `s19` 相比 `s18`，新增的核心能力是什么？它解决的具体问题是什么？
不用手动写工具, 动态接入外部工具插件mcp. 注入工具, 外部服务怎么标准接入
1. 更标准一点可以说：
s19 新增的是通过 MCP 协议动态发现并接入外部工具的能力。
它解决的不是“工具不够多”，而是“外部能力怎么不用改主程序就标准接进来”。

2. 为什么所有外部工具都手写接入不可扩展？外部工具已经定义好了
如果所有外部工具都手写接入，那么每接一个服务都要：
- 重写 schema
- 重写 handler
- 改主工具池
这样扩展成本会线性上升，最后主程序会变成一个不断堆外部适配代码的大杂烩，所以不可扩展。

3. `MCPClient`、`connect_mcp()`、`assemble_tool_pool()` 分别负责什么？
`MCPClient`、 本地mcp客服端, 用来tool call 外部mcp服务工具
`connect_mcp()`、 链接mcp
`assemble_tool_pool()` 本地工具与外部工具组装成工具池
3. 更标准一点是：
   - MCPClient：负责抽象“发现工具 + 调用工具”
   - connect_mcp()：负责连接 server 并把 client 注册到当前连接表
   - assemble_tool_pool()：负责把内置工具和所有已连接 MCP 工具组装成当前真实可用工具池
这里最重要的是：
connect_mcp() 不负责执行业务，assemble_tool_pool() 不负责连接，它们职责是拆开的。


4. 为什么 MCP 工具名要做成 `mcp__server__tool` 这种形式？ mcp多起来. 同名工具可能存在. 为了区分开来.避免调用错误, 给对应的mcp空间前缀
再补一句更完整：
这不是命名习惯，而是命名空间隔离机制。
插件化之后，同名工具是常态，不加前缀迟早冲突。

5. `s19` 提升的是模型能力，还是能力接入能力？为什么？
能力接入能力, 只是新增了外部服务怎么标准接入
模型没变聪明，变化的是 Agent 的能力来源从“固定内置工具”变成了“可动态接入的外部能力池”。
你这一轮可以记成一句话：
s19 不是增加一个工具，而是把“工具从哪里来”这件事协议化了。`

### 第二轮

1. 为什么 `connect_mcp()` 只负责连接和发现，而不是直接执行业务？解耦, 职责拆开
更完整一点是：它负责把“有哪些外部能力可用”先接进系统，但不负责“具体要调用哪个工具做什么事”。这样接入层和执行层就分开了。
2. 为什么 `assemble_tool_pool()` 必须每轮重组，而不是连上一次之后就不管？mcp工具可能更新. 需要动态加载最新的
每轮重组，不只是因为“工具可能更新”，更关键是“当前已连接的 server 集合会变化”。所以工具池本身是运行时状态，不是启动时常量。

3. 为什么 `normalize_mcp_name()` 是安全边界的一部分？
它不是普通字符串美化，而是安全边界的一部分。因为 server 名和 tool 名最终会进入统一命名空间，如果不规范化，就会有：
- 特殊字符问题
- 引用不稳定
- 命名注入风险
4. `s19` 为什么还能继续说 `loop unchanged`？
变化的是 Agent 的能力来源从“固定内置工具”变成了“可动态接入的外部能力池”。

### 第三轮

1. 为什么说 `s19` 的关键不在“插件多了”，而在“工具池变成动态系统”？核心是通过 MCP 协议动态发现并接入外部工具的能力。
答得还不够准。重点不是“又能接更多插件了”，而是“工具集合从静态常量变成运行时动态状态”。

2. 为什么动态工具池会自然冲击 `s10` 的 prompt cache 前提？因为 system prompt 里通常会包含“当前有哪些工具可用”。
一旦工具池变了，prompt 也必须跟着变。
3. `s19` 为什么会自然通向 `s20`？光能接插件还缺什么？系统已经有：
- 计划和任务
- 多 Agent 协作
- worktree 隔离
- 自治认领
- 外部插件能力
这些机制怎么重新合回一个完整 harness。
4. 用你自己的话总结一句：`s19` 的设计本质是什么？不要说 “MCP plugin”。
`s19` 的设计本质是通过 MCP 协议动态发现并接入外部工具的能力。
---

## 参考答案

### 第一轮参考答案

1. `s19` 新增的是通过 MCP 协议动态发现和接入外部工具的能力。它解决的是外部服务扩展成本高、工具接入不统一的问题。

2. 因为每接一个外部服务都要重写 schema、handler 和工具池组装，维护成本会迅速失控。
3. `MCPClient` 负责抽象发现和调用，`connect_mcp()` 负责连接 server 并注册 client，`assemble_tool_pool()` 负责把内置工具和 MCP 工具拼成当前真实可用工具池。
4. 因为不同 server 会有同名工具，必须通过命名空间隔离冲突。
5. 提升的是能力接入能力，不是模型推理能力。

### 第二轮参考答案

1. 因为连接层和业务执行层应该分离，前者负责可用能力发现，后者负责真正的工具调用。
2. 因为当前已连接的 server 会变化，工具池不是静态的。
3. 因为 server 名和工具名最终会进入统一工具命名空间，必须防止特殊字符和命名注入。
4. 因为主循环协议没变，变的是 loop 外围多了动态工具池组装。

### 第三轮参考答案

1. 因为真正的变化是工具集合不再固定，而会根据当前连接状态动态变化。
2. 因为 prompt 里会包含当前可用工具说明，工具池变化就意味着 prompt 也要变化，缓存前提被打破。
3. 因为现在所有机制都已经存在，但还没有统一合回一个完整 harness。
4. `s19` 的设计本质，是把 Agent 的能力来源从固定内置工具升级成可动态发现和组装的外部能力池。
